# Branch Tree 3D

3D version of the branching tree sketch.

- Black environment
- Neon green growing branches
- Branches can stop growing
- Click terrain to plant a tree
- Slow reproduction via seed drops

Run from repo root:

```bash
npx serve .
```

Open:

- `http://localhost:3000/branch-tree-3d/`
